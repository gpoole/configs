source /usr/local/share/antigen/antigen.zsh

source ~/.zshrc-base

# System-specific plugins
# antigen bundle brew

# Full SSH functionality in Kitty terminal
# alias ssh="kitty +kitten ssh"

# PATH="$HOME/Scripts/mac:$PATH"
# PATH="$HOME/Scripts/linux:$PATH"

# eval "$(ssh-agent)" > /dev/null

# Add local config here

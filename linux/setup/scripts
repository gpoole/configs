git clone --recurse-submodules git@github.com:gpoole/scripts.git ~/Scripts

~/Scripts/git-pusheen/git_pusheen_setup.sh

# These paths are in zshrc-template, but add them to .profile too
echo '\nPATH=$HOME/Scripts:$PATH' >> ~/.profile
echo 'PATH=$HOME/Scripts/local:$PATH' >> ~/.profile
echo 'PATH=$HOME/Scripts/linux:$PATH' >> ~/.profile